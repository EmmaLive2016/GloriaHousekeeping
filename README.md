# Gloria's House Keeping Service

Static multi-page website + Netlify serverless functions.
Voice-call booking confirmation (no SMS) and an AI chat assistant.

- Site pages live in `public/`
- Serverless functions live in `netlify/functions/`
- Build config is `netlify.toml` (publish directory = `public`)

Full deployment steps are in **SETUP.md**.
